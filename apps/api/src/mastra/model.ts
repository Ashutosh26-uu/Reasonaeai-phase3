/**
 * The model-router identifier used by the CTO runtime and the local chat
 * harness.
 *
 * `deepseek-flash` is DeepSeek's moving alias for the latest V4 Flash model, so
 * it is pinned here rather than a versioned id.
 */
export const frontierModel = "deepseek/deepseek-flash";
